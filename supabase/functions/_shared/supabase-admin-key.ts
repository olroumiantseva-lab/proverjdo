export type EnvReader=(name:string)=>string|undefined;export function getSupabaseAdminKey(getEnv:EnvReader=(name)=>Deno.env.get(name)):string{const k=getEnv("SUPABASE_SECRET_KEY")?.trim();if(k)return k;const ks=getEnv("SUPABASE_SECRET_KEYS");if(ks){try{const p:unknown=JSON.parse(ks);if(p&&typeof p==="object"&&!Array.isArray(p)){const d=(p as Record<string,unknown>).default;if(typeof d==="string"&&d.trim())return d.trim();}}catch{}}return getEnv("SUPABASE_SERVICE_ROLE_KEY")?.trim()??"";} 