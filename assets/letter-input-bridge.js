(()=>{'use strict';const form=document.getElementById('letter-form'),facts=document.getElementById('letter-facts'),source=document.getElementById('source-text');if(!form||!facts||!source)return;let applying=false;facts.dataset.userFacts=facts.value;facts.addEventListener('input',()=>{if(!applying)facts.dataset.userFacts=facts.value});form.addEventListener('submit',()=>{const user=String(facts.dataset.userFacts??facts.value).trim(),material=String(source.value||'').trim();applying=true;facts.value=[user,material?`Материал из приложенного документа:\n${material}`:''].filter(Boolean).join('\n\n');applying=false},true);})();