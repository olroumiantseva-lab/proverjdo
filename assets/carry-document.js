(()=>{'use strict';function run(){if(new URLSearchParams(location.search).get('source')!=='compose')return;let carry=null;try{carry=JSON.parse(sessionStorage.getItem('proverjdo.carry.document.v1')||'null')}catch{}if(!carry?.text)return;const paste=document.getElementById('source-paste'),source=document.getElementById('source-text'),status=document.getElementById('file-status');if(paste){paste.value=String(carry.text);paste.dispatchEvent(new Event('input',{bubbles:true}))}if(source&&!source.value){source.value=String(carry.text);source.dispatchEvent(new Event('input',{bubbles:true}))}if(status)status.textContent='Готовый документ из предыдущего шага уже добавлен.'}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',run);else run();})();