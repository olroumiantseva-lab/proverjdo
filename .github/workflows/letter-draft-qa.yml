name: Letter Draft QA
on:
  push:
    paths:
      - '.github/workflows/letter-draft-qa.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  qa:
    runs-on: ubuntu-latest
    steps:
      - name: Run QA matrix
        run: |
          curl --fail-with-body --silent --show-error 'https://vhssshjcrirsuiijolwq.supabase.co/functions/v1/letter-draft-qa?token=qa_7f96c2a1d4e84b6a9f31c7025d8e4b13' -o qa-results.json
          python - <<'PY'
          import json
          with open('qa-results.json', encoding='utf-8') as f:
              data=json.load(f)
          print(json.dumps(data, ensure_ascii=False))
          PY
